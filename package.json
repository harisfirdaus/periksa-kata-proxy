{
  "name": "periksa-kata-proxy",
  "version": "1.0.0",
  "description": "Vercel proxy untuk Periksa Kata Chrome Extension",
  "main": "api/check.js",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node test/test-api.js"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}